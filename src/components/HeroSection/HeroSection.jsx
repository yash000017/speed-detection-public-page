import React from 'react';
import './HeroSection.css';

const HeroSection = () => {
  return (
    <section id='home' className="hero">
      <div className="hero-container">
        <div className="hero-title">Revolutionizing Speed Ball Detection</div>
        <div className="hero-description">
          Our cutting-edge technology uses AI and real-time detection to monitor and analyze speed ball activities.
        </div>
      </div>
    </section>
  );
};

export default HeroSection;
